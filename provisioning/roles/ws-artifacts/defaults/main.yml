---
# Staging server the encoded PowerShell payload "downloads" from.
# Randomised per sandbox by the APG entry in variables.yml.
staging_ip: 203.0.113.50

# Where the fake Office binary is planted.
office_dir: 'C:\Program Files\Microsoft Office\root\Office16'
office_stub: 'C:\Program Files\Microsoft Office\root\Office16\WINWORD.EXE'

# Account the scheduled task runs as.
artefact_run_as: SYSTEM
